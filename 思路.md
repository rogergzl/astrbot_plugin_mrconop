# MRCon 插件完整执行逻辑文档

> **版本**: v4.1.1 | **作者**: lindagao | **框架**: AstrBot  
> **仓库**: https://github.com/rogergzl/astrbot_plugin_mrconop

---

## 一、项目概览

`astrbot_plugin_mrcon`（注册名 `mrcon`，显示名 "MRCon 综合管理"）是一个 AstrBot 插件，在 QQ 群中管理 Minecraft 服务器，提供 RCON 远程命令转发、服务器并发查询、SQLite/MySQL 双引擎玩家数据库、在线会话追踪、签到、补偿、群服消息互联、日志监控、事件宏等综合功能。

**核心架构**: `main.py` 作为编排中心，`core/` 目录承载全部业务逻辑（按模块拆分），`web/` 目录承载 Web 管理面板。

---

## 二、文件结构与职责

```
newmrcon/
├── main.py                     # 插件入口，编排中心，命令路由，配置加载
├── __init__.py                  # 根包标识
├── metadata.yaml                # 插件元数据 (v4.1.1)
├── _conf_schema.json             # AstrBot 配置 Schema (10个配置域)
├── web_panel.py                  # Web面板调度入口（委托到 web.server）
├── logo.png                      # 插件图标
│
├── core/                         # 核心业务逻辑模块
│   ├── __init__.py
│   ├── transport.py              # RCON协议 (AsyncRcon短连接 + RconPool长连接池)
│   ├── database.py               # SQLite/MySQL 双引擎数据持久层 (8张表)
│   ├── log_listener.py           # MC日志文件监听器 (LogWatcher + LogListenerManager)
│   ├── log_events.py             # 日志事件处理 (监听分发 + 事件宏执行)
│   ├── online_tracker.py         # 在线时长追踪 (轮询/提醒/踢出/封禁/事件宏)
│   ├── rcon_executor.py          # RCON命令执行器 (节流/锁/审计)
│   ├── relay.py                  # 群服消息互联 (Q2MC/MC2Q/日志驱动)
│   ├── server_manager.py         # MC服务器CRUD + 查询 + 触发器
│   ├── rate_limit.py             # 智能速率限制 (动态递增/白名单)
│   ├── permission.py             # 权限控制 (管理员/白名单/黑名单)
│   ├── audit.py                  # 审计日志 (JSON行格式)
│   ├── player_manager.py         # 玩家绑定/签到/统计
│   ├── compensation.py           # 物品补偿申请/审批
│   ├── exchange.py               # 积分兑换商城
│   ├── lottery.py                # 积分抽奖系统
│   ├── macro.py                  # 宏命令/脚本/自定义命令
│   ├── vote.py                   # 命令投票系统
│   ├── help.py                   # 帮助信息渲染
│   └── utils.py                  # 公共工具 (MC颜色剥离/JSON读写/玩家名校验)
│
└── web/                          # Web管理面板
    ├── __init__.py
    └── server.py                 # 原生asyncio HTTP服务器 + SPA前端 (11+ Tab)
```

---

## 三、插件生命周期

### 3.1 初始化 (`__init__`)

```
MrconPlugin.__init__(context, config)
  ├── 读取 _conf_schema.json 导出的全部配置项，缓存为实例属性
  │   ├── 管理员: admin_qqs, dangerous_commands_blacklist
  │   ├── 速率限制: rate_limit.* (7项)
  │   ├── 通用: select_ttl, rcn_persistent, keepalive_interval, idle_disconnect, scripts_dir
  │   ├── 查询: show_*, auto_cleanup_days
  │   ├── 在线追踪: enabled, poll_mode, query_method, notify/kick/ban 阈值与模板
  │   ├── 消息互联: enabled, group_to_mc, mc_to_group, mc_to_group_log, require_msay, format_*
  │   ├── 玩家DB: checkin/streak 积分, 补偿开关/白名单/黑名单, storage_mode/read_source
  │   ├── Web面板: enabled, host/port, password, session_timeout
  │   └── 宏定义: macro_definitions 列表
  │
  ├── 初始化数据目录: data/plugin_data/mrcon/
  │
  ├── 加载持久化 JSON 文件 (safe_json_read):
  │   ├── mcserv.json           → 全局服务器列表
  │   ├── server_templates.json  → 服务器模板
  │   ├── group_data.json        → 分群映射 {group_id: [server_names]}
  │   ├── group_names.json       → 群名映射
  │   ├── cmd_templates.json     → 命令模板
  │   ├── online_triggers.json   → 上线触发器
  │   ├── script_settings.json   → 脚本启停状态
  │   ├── quick_cmd_settings.json→ 快捷命令启停
  │   ├── custom_cmds.json       → 自定义命令映射
  │   ├── macro_definitions.json → 宏命令定义
  │   ├── event_macros.json      → 事件宏定义
  │   ├── relay_overrides.json   → 群互联覆盖
  │   ├── tracker_overrides.json → 追踪分群覆盖
  │   ├── general_overrides.json → 通用分群覆盖
  │   ├── server_log_configs.json→ 服务器日志配置
  │   └── ranking_state.json     → 排行清理状态
  │
  ├── 初始化 Database (SQLite/MySQL 双引擎)
  │   ├── 建表: players / online_sessions / online_state / compensations
  │   │         / exchange_items / lottery_prizes / lottery_wins
  │   └── JSON→SQLite 迁移 (从旧版 mc_players.json / compensations.json)
  │
  ├── 初始化在线缓存 _online_cache = {}
  │
  └── 注册 "薄包装" 方法 (self.xxx → _core_xxx 桥接)
```

### 3.2 启动 (`initialize`)

```
MrconPlugin.initialize()
  ├── 启动在线追踪循环 _online_tracker_loop() (asyncio.create_task)
  ├── 初始化日志监听器 _init_log_listeners() (asyncio.create_task)
  │   └── 为每个配置了 log_path 的服务器创建 LogWatcher
  └── 启动 Web 管理面板 (asyncio.create_task)
      └── WebServer(host, port, password).start()
```

### 3.3 运行中 — 消息处理入口

```
AstrBot 框架收到 QQ 群消息
  → MrconPlugin 的 @filter.command / @filter.event_message_type 路由匹配
  → 触发对应的处理方法
```

### 3.4 终止 (`terminate`)

```
MrconPlugin.terminate()
  ├── 关闭 Web 服务器
  ├── 取消在线追踪循环
  ├── 停止日志监听器
  └── 关闭 RCON 连接池 → 关闭数据库连接
```

---

## 四、全部 QQ 命令及执行流程

### 4.1 服务器查询模块

| 命令 | 别名 | 功能 | 执行流程 |
|------|------|------|----------|
| `/mc` | `查询` | 查询本群所有MC服务器状态 | 获取本群服务器列表 → 并发 mcstatus 查询 → `_format_server_status` 格式化 → 可选渲染在线玩家图片(PIL) → 返回群聊 |
| `/mcget <名>` | `服详情` | 单服详情 | 匹配服务器名 → mcstatus 查询 → 格式化（含版本/延迟/在线玩家列表/MOTD） |
| `/mclist` | `服列表` | 服务器列表 | 列出本群所有服务器名(编号) |
| `/mcadd <名> <IP:端口>` | `加服` | 添加服务器 | 解析 IP/端口 → 检查重复 → 添加到 mcserv.json + group_data |
| `/mcdel <名>` | `删服` | 删除服务器 | 匹配服务器 → 从本群移除 → 全局无引用则删除 |
| `/改服 <名> [新名] [新址]` | `mcup` | 更新服务器 | 更新名称/地址/端口 |
| `/共享服` / `/取消共享` | `mcshare/unshare` | 群间共享 | 将服务器共享到其他群/取消共享 |
| `/清理服` | `mccleanup` | 清理失效服务器 | 遍历服务器 → 测试连接 → 移除不可达的 |
| `/查询设置` | `mcset` | 查询显示项开关 | 展示 show_* 配置项的当前状态 |

### 4.2 RCON 远程命令模块

| 命令 | 别名 | 功能 | 执行流程 |
|------|------|------|----------|
| `/mrcon <命令>` | `执行`/`mcmd` | 转发 RCON 命令 | 权限检查 → 危险命令黑名单匹配 → 服务器选择 → 速率限制 → RCON发送 → 审计 → 返回结果 |
| `/选服 <编号>` | `rc选`/`rcsel` | 多服务器选择 | 当本群>1台服务器时，弹出编号列表供选择，超时自动取消 |
| `/宏 <名> [参数]` | `rcm`/`rcmacro` | 执行宏命令 | 加载 macro_definitions → 匹配名称 → 展开命令列表 → 逐条 RCON 执行 |
| `/脚本 <文件>` | `rcs`/`rcscript` | 执行脚本文件 | 读取 scripts_dir 下脚本 → 逐行 RCON 执行 |
| `/rc自定 <别名>` | `rccustom` | 自定义命令映射 | 将别名映射到预设 RCON 命令 |

### 4.3 命令投票系统

| 命令 | 功能 | 执行流程 |
|------|------|----------|
| `/rc赞同` | 赞同当前投票 | 记录投票 → 检查是否达到阈值 |
| `/rc反对` | 反对当前投票 | 记录投票 → 检查是否触发否决 |
| `/rc通过` | 管理员强制通过 | 管理员立即执行投票命令 |
| `/rc否决` | 管理员强制否决 | 管理员立即取消投票 |

**投票机制**: 公开命令自动启动投票 → 超时后结算 → 赞同≥阈值则执行 / 未达到则否决 → 平票策略(fail/pass/admin)

### 4.4 在线时长追踪模块

| 命令 | 功能 | 执行流程 |
|------|------|----------|
| `/在线时长 [服\|玩家]` | 在线排行 | 查询 online_sessions 表 → 按累计/会话时长排行 → 渲染为文本/图片 |
| `/在线提醒` | 本群追踪提醒设置 | 读取/修改 tracker_overrides 分群覆盖 |

### 4.5 玩家数据库模块

| 命令 | 别名 | 功能 | 执行流程 |
|------|------|------|----------|
| `/绑定 <MC_ID>` | `bind` | 绑定MC账号 | 校验玩家名 → 原子绑定(UPSERT) → 写入 players 表 |
| `/签到` | `checkin` | 每日签到 | 检查今日是否已签 → 原子加分(+=) → 连续签到奖励 → 返回积分 |
| `/我的` | `mystats` | 个人统计 | 查询玩家积分/签到天数/连续签到/VIP等级 |
| `/理赔 <命令>` | `赔`/`comp` | 物品补偿申请 | 白名单检查 → 黑名单词汇驳回 → 写入 compensations 表(status=pending) |
| `/理赔列表` | | 查看补偿列表 | 按状态筛选 compensations |
| `/同意理赔 <ID>` | | 同意补偿 | 管理员 → 执行 RCON 命令 → 更新状态为 approved |
| `/拒绝理赔 <ID>` | | 拒绝补偿 | 管理员 → 更新状态为 rejected |
| `/兑换列表` | `shop` | 查看兑换项 | 列出 exchange_items |
| `/兑换 <ID>` | `buy` | 积分兑换 | 检查积分 → 事务扣分+执行RCON |
| `/抽奖` | | 积分抽奖 | 检查积分 → 概率抽取 → 事务扣分+记录中奖 |
| `/奖品列表` | | 查看奖品 | 列出 lottery_prizes |
| `/兑奖 <ID>` | | 兑换奖品 | 验证中奖记录 → 执行RCON → 标记已兑 |
| `/我的中奖` | | 查看中奖记录 | 列出 lottery_wins |

### 4.6 群服消息互联模块

| 命令 | 别名 | 功能 | 执行流程 |
|------|------|------|----------|
| `/msay <文本>` | `群说` | 主动转发到MC | 格式化 → RCON `tellraw @a` 命令 |
| `/消息互通 [模式\|开关\|格式\|日志格式\|服\|重置]` | `relay` | 群服互联配置 | 管理 relay 开关/模式(off/global/custom)/格式/日志类型前缀/绑定服务器 |

---

## 五、核心数据流详解

### 5.1 在线追踪数据流

```
定时器 tick (按 poll_mode 决定间隔)
  │  frequent: 10-30秒    smart: 动态调整    custom: 自定义
  ▼
_online_tracker_loop()
  │
  ├── 遍历所有群的服务器映射 (group_servers)
  │   └── 对每个服务器:
  │       ├── _get_online_player_list()
  │       │   ├── query_method=rcon  → RCON "list" 命令
  │       │   └── query_method=mcstatus → mcstatus 协议查询
  │       │
  │       ├── 与 _online_cache[server] 对比，发现上下线变化
  │       │
  │       ├── 【新玩家上线】
  │       │   ├── 记录 login_at 时间戳
  │       │   ├── save_online_state(DB) → online_state 表
  │       │   ├── 自动创建玩家记录 (players 表)
  │       │   ├── 检查上线触发器 (_check_online_triggers)
  │       │   └── 事件宏: player_join 类型触发
  │       │
  │       ├── 【持续在线】(每次轮询)
  │       │   ├── 计算 session_mins (本次会话时长)
  │       │   ├── 累计模式: 叠加历史 online_sessions 时长
  │       │   ├── 检查通知节点 (配置的分钟阈值)
  │       │   │   ├── QQ群通知 (支持 @玩家 / @管理员 / 纯文本 / 自定义格式)
  │       │   │   └── 游戏内通知 (RCON "say" 命令)
  │       │   ├── 超阈值 → 执行踢出 (RCON "kick" + 可选 "tempban")
  │       │   │   └── 占位符: {player} {hours} {minutes} {reason}
  │       │   ├── 事件宏: player_online_duration 类型 (session/cumulative 模式)
  │       │   └── 更新 _online_cache 状态 (notified/kicked)
  │       │
  │       └── 【玩家下线】
  │           ├── add_online_session(DB) → online_sessions 表 (start_ts→end_ts)
  │           ├── remove_online_state(DB) → 清理 online_state 表
  │           ├── 清理 _online_cache
  │           └── 事件宏: player_leave 类型触发
  │
  └── 排行数据清理 (按 ranking_reset_hours 周期)
      └── 删除超出记录时长的旧 online_sessions 记录
```

### 5.2 RCON 执行数据流

```
用户发送 /mrcon <命令>
  │
  ├── cmd_mrcon() [vote.py]
  │   ├── 权限检查 (_core_is_allowed)
  │   ├── 危险命令黑名单匹配 (_core_match_dangerous)
  │   ├── 多服务器选择 (>1台时弹出编号选择，超时自动取消)
  │   └── 公开命令 + vote_enabled → 启动投票流程
  │
  ├── execute_and_reply() [rcon_executor.py]
  │   ├── 速率限制检查 (_core_check_rate)
  │   │   ├── 管理员/白名单: 仅受 200ms 硬编码最低延迟
  │   │   └── 普通用户: 动态递增 (基础→递增→最大 间隔ms)
  │   ├── 异步锁 (_core_acquire_lock)
  │   │   └── 按 (group_id, user_id) 键加锁，保证串行
  │   └── rcn_send() → transport_send()
  │       ├── rcn_persistent=true → RconPool 长连接池
  │       │   ├── 按 (host, port, password) 查找/创建连接
  │       │   ├── 复用连接 → 心跳维护 (定期 "list")
  │       │   ├── 空闲超时自动释放
  │       │   └── 断线自动重连
  │       └── rcn_persistent=false → AsyncRcon 短连接
  │           ├── connect() → authenticate() → send_cmd() → close()
  │           └── 每次新建，用完即关
  │
  └── 审计记录 (_audit)
      └── JSON行写入 audit.log
          ├── cmd: {ts, type:"cmd", group_qq, user_qq, server, command, result, duration_ms}
          ├── web: {ts, type:"web", ...}
          └── web_rcon: {ts, type:"web_rcon", ...}
```

**RCON 协议细节** (`core/transport.py`):
- 基于原生 `asyncio.open_connection` TCP 连接
- 协议格式: 4字节长度 + 4字节请求ID + 4字节类型(2=命令/3=登录) + 载荷 + 2字节空终止
- 认证: 类型3请求，密码为空则跳过认证
- 响应: 读取长度头 → 读取完整载荷 → 解码返回

### 5.3 消息互联数据流

#### 5.3.1 群 → MC (Q2MC)

```
QQ群消息 (非命令)
  │
  ├── _on_group_message() [relay.py]
  │   ├── 检查 relay 开关 (off → 不处理)
  │   ├── require_msay 检查 (仅允许 /msay 命令转发)
  │   ├── 获取本群绑定服务器 (global 模式 / custom 模式)
  │   ├── 格式化消息 (format_group 模板)
  │   │   └── 占位符: {group} {qq} {nickname} {message}
  │   └── RCON "tellraw @a [格式化JSON]"
  │       └── 目标: MC 全服公屏
```

#### 5.3.2 MC → 群 (MC2Q, 日志驱动)

```
LogListenerManager 轮询 (0.5秒间隔)
  │
  ├── LogWatcher.poll() 每个服务器日志文件
  │   ├── tail-f 模式: 维护文件位置指针
  │   ├── inode 检测: 自动处理日志轮转 (logrotate)
  │   └── 新增行 → 分类正则匹配
  │       ├── 内置20种分类: chat/command/player_join|leave|death|advancement|
  │       │                  kick|respawn|death_pvp|item_get|server_start|stop|
  │       │                  reload|boss_kill|tps_low|tps_critical|memory_high|
  │       │                  server_perf_issue|system
  │       └── 用户自定义正则 (Web面板配置)
  │
  ├── 全量日志缓冲区 (2000条 deque)
  │   └── Web面板日志查看器数据源
  │
  └── _on_log_event() [log_events.py]
      ├── 5秒去重窗口 (同服务器+同类型+同玩家)
      ├── 中转到 _relay_log_event_to_groups()
      │   ├── 匹配群 relay 配置 (按服务器名)
      │   ├── 按事件类型过滤
      │   ├── 格式化 (log_type_prefixes 前缀 + 消息模板)
      │   │   └── 例: "☠ [死亡] {player} 死了" → "💀 [死亡] Steve 被僵尸杀死了"
      │   └── 发送到对应QQ群
      │
      └── 触发 _execute_event_macros()
          ├── 遍历 _event_macros 列表
          ├── 匹配条件:
          │   ├── enabled=true
          │   ├── event_type 匹配 (支持子事件: player_chat, vip_join, vip_death等)
          │   ├── player_name 过滤 (支持通配符 *)
          │   ├── event_param 关键词匹配
          │   ├── conditions 条件组 (and/or gate_mode)
          │   ├── cooldown 冷却未到期
          │   └── 频率限制 (max_triggers/trigger_window)
          ├── pre_delay 等待 → 执行 RCON commands
          │   ├── 变量替换: {player} → 玩家名 / {PLAYER} → 大写玩家名
          │   └── 每命令可选独立 delay
          ├── if qq_message: → 发送QQ通知到绑定群
          └── post_delay 等待 → 设置 cooldown 到期时间
```

**日志事件类型派生关系**:
```
server_chat → player_chat
player_join → player_first_join / admin_join / vip_join
player_leave → vip_leave
player_death → vip_death / player_first_death
```

### 5.4 指令投票数据流

```
用户发送公开命令 (如 /mrcon time set day)
  │
  ├── vote.py 拦截
  │   ├── 检查 vote_enabled 是否开启
  │   └── 自动创建投票上下文: {cmd_text, group_id, expires_at, yes=[], no=[]}
  │
  ├── 群成员:
  │   ├── /rc赞同 → 记录到 yes 列表，检查阈值
  │   └── /rc反对 → 记录到 no 列表，检查阈值
  │
  ├── 超时结算:
  │   ├── yes 占比 ≥ 阈值 → 执行命令
  │   ├── yes 占比 < 阈值 → 否决
  │   └── 平票:
  │       ├── policy=fail → 否决
  │       ├── policy=pass → 通过执行
  │       └── policy=admin → 等待管理员裁决
  │
  └── 管理员:
      ├── /rc通过 → 立即执行
      └── /rc否决 → 立即取消
```

---

## 六、Web 管理面板

### 6.1 技术架构

- **后端**: 原生 `asyncio` TCP HTTP 服务器（无第三方HTTP框架）
- **前端**: 纯 HTML + CSS（暗色主题）+ JavaScript SPA（无框架）
- **认证**: PBKDF2-SHA256 密码哈希 + session token 管理
- **防爆破**: 1分钟5次失败锁定1分钟
- **默认地址**: `http://{host}:9949`
- **配置项**: `web_panel.enabled/host/port/password/session_timeout`

### 6.2 功能 Tab 列表

| Tab | 主要功能 |
|-----|---------|
| **仪表盘** | 全局统计卡片（群/服务器/玩家/在线/补偿/宏数量）+ 模块状态 |
| **服务器管理** | 群列表 + 服务器增删改查 + 模板管理 + 群名配置 |
| **群服互联** | 全局互联开关 + 分群 relay 配置 + 格式编辑器 + 日志类型前缀/过滤 |
| **在线追踪** | 全局追踪设置 + 分群覆盖（通知/提醒/踢出/@模式/封禁/命令模板） |
| **玩家管理** | 玩家列表 + 搜索/添加/编辑/删除 + VIP等级 + 导入 |
| **补偿管理** | 状态筛选 + 通过/拒绝操作 |
| **积分兑换** | 分群兑换项增删改查 + 启用/禁用 |
| **抽奖管理** | 分群奖品增删改查（概率/数量/消耗积分/RCON命令） |
| **在线列表** | 卡片视图 + 时长进度条 + 排行 + 刷新/重置 |
| **审计日志** | 分类筛选（全部/命令/Web操作），最近100条 |
| **宏命令** | 宏CRUD + 事件宏可视化编辑（条件组/冷却/频率/在线时长触发） |
| **脚本与触发器** | 脚本增删改 + 上线触发器 + 自定义命令映射 |
| **日志查看器** | 实时日志列表 + 分类正则JSON编辑 |
| **快捷命令** | 预设命令格 + 命令模板 + 自定义RCON直发 |
| **全局设置** | 完整可编辑表单（速率/DB/追踪/查询/互联/危险命令等） |

### 6.3 API 路由体系

```
认证:  POST /api/login | /api/logout | GET /api/auth
仪表盘: GET /api/status
服务器: GET|POST|PUT|DELETE /api/servers/*
群管理: GET|PUT|DELETE /api/groups* | /api/group-names/*
模板:   GET|POST|PUT|DELETE /api/server-templates/*
互联:   GET|PUT /api/relay/*
追踪:   GET|PUT|DELETE /api/tracker/*
玩家:   GET|POST|PUT|DELETE /api/players/*
补偿:   GET|POST /api/compensations/*
兑换:   GET|POST|PUT|DELETE /api/exchange/*
抽奖:   GET|POST|PUT|DELETE /api/lottery/*
在线:   GET|POST /api/online*
审计:   GET /api/audit*
宏:     GET|POST|PUT|DELETE /api/macros* | /api/event-macros*
脚本:   GET|POST|PUT|DELETE /api/scripts*
触发器: GET|POST|PUT|DELETE /api/online-triggers*
模板:   GET|POST|PUT|DELETE /api/cmd-templates*
自定:   GET|POST|PUT|DELETE /api/custom-cmds*
快捷:   GET|PUT /api/quick-cmds/* | /api/quick-cmd-settings/*
RCON:   POST /api/rcon/exec
配置:   GET|PUT /api/config/*
日志:   GET /api/log-viewer/*
```

---

## 七、配置 Schema 概览

| 配置域 | 关键配置项 |
|--------|-----------|
| `admin` | bot_admin_qqs, dangerous_commands_blacklist, rate_limit(7项) |
| `general` | select_ttl, rcn_persistent, rcon_keepalive_interval, rcon_idle_disconnect, scripts_dir, event_macro_game_prefix, game_notify_prefix, admin_mc_ids |
| `query` | show_address_port/version/latency/online_count/players_detail/render_online_image, auto_cleanup_days |
| `online_tracker` | enabled, poll_mode(frequent/smart/custom), query_method(rcon/mcstatus), notify/kick/ban 阈值与模板, duration_mode(session/cumulative), ranking_reset_hours, log_patterns |
| `relay` | enabled, group_to_mc, mc_to_group, mc_to_group_log, require_msay, format_group/mc, log_type_prefixes, group_settings |
| `player_db` | enabled, checkin/streak/new_player 积分, 补偿开关/白名单/黑名单, storage_mode/read_source(local/external/dual), 外部DB连接参数 |
| `online_db` | 在线数据库独立存储配置（优先读取 player_db） |
| `macro_definitions` | 宏命令列表 (name + commands) |
| `web_panel` | enabled, host/port, password(PBKDF2-SHA256), session_timeout, log_viewer_refresh_ms |
| `groups` / `group_names` / `servers` | 群列表、群名映射、服务器名称列表 |

---

## 八、数据持久化

### 8.1 SQLite 数据库 (`data/plugin_data/mrcon/mrcon.db` / MySQL)

| 表名 | 关键字段 | 说明 |
|------|---------|------|
| `players` | qq_id, mc_id, points, vip_level, checkin_* | 玩家主表 |
| `online_sessions` | server_name, player_name, start_ts, end_ts | 在线会话记录 |
| `online_state` | server_name, player_name (UNIQUE) | 当前在线状态 |
| `compensations` | qq_id, rcon_cmd, status(pending/approved/rejected) | 补偿申请 |
| `exchange_items` | group_id, name, cost_points, rcon_cmd | 积分兑换项 |
| `lottery_prizes` | group_id, name, probability, count, cost_points | 抽奖奖品 |
| `lottery_wins` | qq_id, prize_data, redeemed | 中奖记录 |

**存储模式**: local(本地SQLite) / external(外部SQLite/MySQL) / dual(双写+读写分离)

### 8.2 JSON 持久化文件 (`data/plugin_data/mrcon/`)

| 文件 | 用途 |
|------|------|
| `mcserv.json` | 全局服务器列表 [{name, address, port, rcon_password, ...}] |
| `group_data.json` | 分群映射 {group_id: [server_names]} |
| `server_templates.json` | 服务器模板 |
| `cmd_templates.json` | 命令模板 |
| `online_triggers.json` | 上线触发器 |
| `macro_definitions.json` | 宏命令定义 |
| `event_macros.json` | 事件宏定义 |
| `relay_overrides.json` | 群互联覆盖 |
| `tracker_overrides.json` | 追踪分群覆盖 |
| `general_overrides.json` | 通用分群覆盖 |
| `server_log_configs.json` | 服务器日志配置 |
| `script_settings.json` | 脚本启停状态 |
| `quick_cmd_settings.json` | 快捷命令启停 |
| `custom_cmds.json` | 自定义命令映射 |
| `ranking_state.json` | 排行清理时间戳 |

### 8.3 审计日志 (`data/plugin_data/mrcon/audit.log`)

JSONL 格式，每行一条记录，三种类型:
- `cmd`: 群聊 RCON 命令执行记录
- `web`: Web 面板操作记录
- `web_rcon`: Web 面板发起的 RCON 命令记录

---

## 九、关键地址与路径汇总

| 项目 | 地址/路径 |
|------|----------|
| **Web 管理面板** | `http://{web_panel.host}:{web_panel.port}` (默认 `http://0.0.0.0:9949`) |
| **插件数据目录** | `data/plugin_data/mrcon/` |
| **SQLite 数据库** | `data/plugin_data/mrcon/mrcon.db` |
| **审计日志** | `data/plugin_data/mrcon/audit.log` |
| **MC 服务器配置** | `data/plugin_data/mrcon/mcserv.json` |
| **脚本目录** | `scripts_dir` 配置指定的目录 |
| **代码仓库** | https://github.com/rogergzl/astrbot_plugin_mrconop |
| **AstrBot 框架** | 基于 `astrbot.api.star.Star` 插件体系 |

---

## 十、版本管理

| 版本 | 日期 | 核心变更 |
|------|------|---------|
| v3.x | 2026-06 前 | 单体架构 (main.py 2057行)，功能模块未拆分 |
| v4.0.0 | 2026-06-09 | 完全重构：模块化拆分到 `core/` + `web/`，新增 Web 管理面板 |
| v4.1.0 | 2026-06-09 | tempban 原生化，VIP事件细化，封禁命令模板占位符 |
| v4.1.1 | 2026-06-09 | 修复 log_events 回调签名、踢出原因占位符统一、代码清理 |

完整版本日志见 [DEVLOG.md](file:///d:/BC/cs/newmrcon/DEVLOG.md)。
