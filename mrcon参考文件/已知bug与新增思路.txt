在线玩家列表悬浮的详情信息中  短的悬浮详情信息会被长的挡住,建议将这个悬浮信息放在最顶层(办结)

是否每个玩家绑定仅能绑定自己(管理员是否可以绑定帮助别人绑定,管理员分为全局与群级别,全局管理员什么都可以,群级别只能改群内玩家绑定)(办结)

允许按群查看在线玩家列表，服务器管理中独立群允许配置是否在web管理开关(办结)

允许点击查看单个玩家在线数据(办结)

在线玩家数据太多，存储方式改为本地数据库，在已有数据库添加条目，并且在web管理面板添加关联外部数据库功能，允许配置存储与读取数据库地址，存储允许配置仅本地数据库，仅外部数据库，双数据库同时存储(办结)

添加积分兑换功能,允许玩家积分兑换内容,仅web端可配置,需要选择独立群配置,无全局配置(办结)

将D:\BC\cs\ing\astrbot_plugin_mrcon\mrcon\astrbot_plugin_mc_lottery 的抽奖逻辑添加到mrcon插件中,依旧使用已有数据库内容(玩家,积分等)(配置项目更改到web端或群管理命令配置),并且添加抽奖逻辑,积分抽奖开关,常驻开启或关闭,允许玩家使用签到等获得的积分抽奖(办结)

统一度量衡,检查全局代码中可以使用同一数据库,地址,命令读取地址等地方的内容不要使重复获取命令,导致出现全端数据不统一(比如web端修改的内容命令段不认等,多个玩家积分地址等)(办结)

完整更新rchelp与md(办结)

web全局设置保存点击依旧没反应,但是切换栏位直接保存报这个错误Cannot read properties of null (reading 'value')(办结)

日志查看其ss能识别到日志但下面的分类加载不出不来，是不是参数问题,是否可以将识别参数设置为可修改的持久化参数（就像快捷命令里面的参数一样，可配置）(办结)

日志查看器的开关改成一个提示就行,没必要有关闭功能,反正不填写路径也不是别,全局管理的开关主要起提示作用,关选项改成是,作用关联到开,日志查看器界面的开关直接删掉(web端)(办结)

添加web报错方式,将如果报错   MRCon 控制台 正在连接...   更改为直接打印报错信息或者浏览器控制台报错(办结)

v3.39.0 时的修改导致web不可用执行修复,还有,现在的 新建事件宏 可以实现 事件x和事件y同时满足才执行么或者事件x与事件其中一项不满足则执行,反正总结就是事件的与或非三门实现布尔逻辑的「完备集」(办结)

在线追踪 的  全局追踪设置 与 群独立配置 都应该有一个新配置,就是在线时长监控提醒与踢出的的规则为在x小时内连续在线还是单次在线时长x小时才执行后续规则(已有在线时长监控规则)(办结)

bug 添加配置好日志事件宏一开启或关闭就会所有数据全消失,老规矩,持久化到json,并且允许导出与导入对应配置好的json项,并且检查修复配置好的日志宏事件为什么会所有配置项都消失.(办结)

玩家管理里面应该显示vip等级的列(办结)

为什么会莫名奇妙加载到Abhorrent这些玩家id,Abhorrent	[08Jun2026 22:20:12.674] [Server thread/INFO] [net.minecraft.world.entity.LivingEntity/]: Named entity Apostle['Leprosy the Abhorrent'/1317297, l='ServerLevel[world]', x=175.70, y=5.00, z=41.61] died: Leprosy the Abhorrent died  日志里是这个(有自动加载在线玩家逻辑,但是在线玩家里没有这东西,只有玩家管理里卖弄出现了)(办结)

玩家管理中可设置的vip等级上限可以无上限设置,默认上限应该是3(办结)

编辑事件宏的事件类型添加玩家复活(读日志可以查看到的话)(办结)

编辑事件宏 的 事件类型 前后都允许添加延时(增加了延时后在延时下面增加相应填写参数与rcon命令框)(以达到玩家加入 延时5s 执行命令)(办结)

(索引):169  PUT http://192.168.31.60:9949/api/event-macros/0 500 (OK)
pjt @ (索引):169
doEmSave @ (索引):337
onclick @ (索引):1
(索引):169 Uncaught (in promise) Error: {"error": "name 'WebPanel' is not defined"}
    at pjt ((索引):169:145)
    at async doEmSave ((索引):337:1284)
pjt @ (索引):169
await in pjt
doEmSave @ (索引):337
onclick @ (索引):1
    3.45.0与v3.45.1版本后的保存报错,编辑事件宏保存出现的报错(已办结)
	
	
添加web在线时长过长的玩家被暂时封禁时的显示,以及封禁时间与理由

📋 审计日志中应该有web执行的玩家封禁的记录与原因(比如web在线时长封禁)

在线时长提醒的自动封禁功能 应该只执行一次,接触封禁后重新计算时长(自动定时解除封禁或者封禁有时间没生效)